# 📌 1. Функция загрузки данных
def load_data(path):
  dataset = pd.read_excel(path, header=0, usecols='B,C,D,F,G:Z', na_filter=True, na_values='Bad Input')
  dataset = dataset.dropna()
  print(dataset.shape)
  return dataset


# 📌 2. Функция для фильтрации данных
def filter_data(df):
  df = df[(df['shaft speed] > 2720) & (df['shaft speed'] < 2770) & (df['vibration'] < 5.3) & (df['vibration'] > 3.3)]
  print(df.shape)
  return df
  

# 📌 3. Функция обучения модели
def train_model(X_train, y_train):
  model = RandomForestRegressor(
    n_estimators=50, criterion='friedman_mse',
    min_samples_split=2, min_samples_leaf=1,
    bootstrap=True, random_state=0
  )
  model.fit(X_train, y_train)
  return model


#📌 4. Функция для оценки модели
def evaluate_model(model, X_test, y_test):
  predictions = model.predict(X_test)
  mse = mean_squared_error(y_test, predictions)
  mae = mean_absolute_error(y_test, predictions)
  r2 = r2_score(y_test, predictions)

  print(f" 📊 Model Evaluation Metrics:")
  print(f" - MSE: {mse:.3f}")
  print(f" - MAE: {mae:.3f}")
  print(f" - R2 Score: {r2:.3f}")

  return predictions


# 📌 5. Функция  для отображения степени вляиния параметров
def plot_feature_importance(model, feature_names):
  feature_importance_df = pd.DataFrame({
    'Feature': feature_names,
    'Importance': model.feature_importances_
  }).sort_values(by='Importance', ascending=False)

  #вывод в консоль
  print("\n🔹 Feature Importances:")
  print(feature_importance_df)

 #построение графика PartialDependenceDisplay
  features = [3, 2, (3, 2)]

  #fig, ax = plt.subplots()

  # Plot partial dependence for shaft speed and motor current
  display = PartialDependenceDisplay.from_estimator(
    model, 
    test_features,  # Use the training data
    features=features,
    feature_names=feature_names,# Feature indices
    #kind='both',  # Plot individual and average effects
    grid_resolution=50,  # Higher for smoother plots
    line_kw={"markersize": 0},  # Remove data distribution markers
  )

  # Add grid to all plots
  for pd_ax in display.axes_.ravel():
      pd_ax.grid(True, linestyle="--", alpha=0.6)  # Add grid with custom styling

  # Extract the collection from the display for the 2D plot
  for pd_ax in display.axes_.ravel():
      # Ensure you're targeting the correct 2D axis
      if hasattr(pd_ax, "collections") and pd_ax.collections:
          colorbar = pd_ax.figure.colorbar(pd_ax.collections[0], ax=pd_ax, orientation="vertical")
          colorbar.set_label("Predicted Vibration Level")

  plt.show()



# --- 📌 основной код ---


# 📍 1. Загрузка данных
path = 'path'
dataset = load_data(path)
print(" ✅ Data Loaded Successfully\n", dataset.head())


# 📍 2. Разделение данных на train/test
train_dataset, test_dataset = train_test_split(dataset, test_size=0.1, random_state=1)


# 📍 3. Фильтрация данных
train_dataset = filter_data(train_dataset)
test_dataset = filter_data(test_dataset)


# 📍 4. Разделение на признаки и целевой показатель
train_labels = train_dataset.pop('vibration')
test_labels = test_dataset.pop('vibration')
feature_names = train_dataset.columns


# 📍 5. Нормализация данных (RandomForestRegressor не требует нормализации)
#scaler = MinMaxScaler()
#train_features = scaler.fit_transform(train_dataset)
#test_features = scaler.transform(test_dataset)

# Используем без нормализации
train_features = train_dataset  
test_features = test_dataset


# 📍 6. Обучение модели
model = train_model(train_features, train_labels)


# 📍 7. Model evaluation
predictions = evaluate_model(model, test_features, test_labels)


# 📍 8. Features importance analysis
plot_feature_importance(model, feature_names)

